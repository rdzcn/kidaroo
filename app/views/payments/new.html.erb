<div class="payment-new-main-container">
  <div class="backgrond-payment"></div>
 <div class="container">
  <div class="row">

    <div class="col-xs-12 col-sm-6 col-sm-offset-3">
      <div class="payment-new-card">
        <h1> Thank you for choosing</h1>
        <%= image_tag "logo.png", class: "logo-confirmation" %>

        <h3>Your activity: </h3>
        <p><%= @booking.event.activity.title.capitalize %></p>
        <p><%= @booking.event.start_date.strftime("%A %e %B ") %></p>
        <p><%= @booking.event.activity.price.to_i %> EUR</p>


        <!-- <article>
          <label class="amount">
            <span>Amount: <%= humanized_money_with_symbol(@booking.amount) %></span>
          </label>
        </article>

        <hr class="payment-line">
        <p><%= @booking.event.activity.title %></p>
        <p> Your event begins at:  <b><%= @booking.event.start_date %> </b></p> -->

        <%= form_tag booking_payments_path(@booking) do %>



        <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
        data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
        data-name="My Booking"
        data-email="<%= current_user.email %>"
        data-amount="<%= @booking.amount_cents %>"
        data-currency="<%= @booking.amount.currency %>"></script>
        <% end %>
      </div>
    </div>
  </div>
</div>
</div>
